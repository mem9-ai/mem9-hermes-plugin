manifest_version: 1
name: mem9
version: 0.2.1
description: "Mem9 - server-side fact extraction, semantic search, and explicit memory CRUD tools."
# MemoryProvider lifecycle hooks implemented:
#   on_session_end  — full smart ingest at session boundary
#   on_pre_compress — session summary before context compression
# These are invoked by MemoryManager via the ABC interface, not plugin.yaml.
